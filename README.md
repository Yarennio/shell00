# 🚀 42 Piscine | Shell00 Projesi: Unix/Linux Temelleri

## 📚 Proje Hakkında

Bu depo, 42 Okulu (École 42) Piscine eğitim sürecinin başlangıç modüllerinden biri olan **Shell00** projesi kapsamında tamamladığım tüm alıştırmaları içerir. Projenin temel amacı, **Unix/Linux tabanlı işletim sistemlerinde** komut satırı arayüzü (CLI) kullanımına giriş yapmak ve temel kabuk (Shell) komutlarına hakimiyet kazanmaktır.


---

## ⚙️ Kapsanan Temel Konular

Shell00 modülü, aşağıdaki temel kavramlar ve beceriler üzerine odaklanmıştır:

* **Temel Komut Satırı Kullanımı:** Dosya ve dizin yönetimi (`ls`, `cd`, `pwd`, `man`).
* **Dosya İşlemleri:** Dosya ve dizin oluşturma (`touch`, `mkdir`), kopyalama (`cp`), taşıma/yeniden adlandırma (`mv`).
* **Dosya İzinleri (Permissions):** Kullanıcı, grup ve diğer kullanıcılar için okuma, yazma ve çalıştırma izinlerinin (`rwx`) `**chmod**` komutu ile ayarlanması.
* **Sürüm Kontrolü (Git):** Temel `git` komutları (`commit`, `add`, `push`, `.gitignore` oluşturma).
* **Uzaktan Bağlantı:** Güvenli Kabuk (SSH) anahtarı oluşturma (`**ssh-keygen**`) ve kimlik doğrulama.
* **Fark ve Yama Dosyaları:** İki dosya arasındaki farkı (`**diff**`) hesaplama ve yama dosyası (`patch`) oluşturma mantığı.
* **Arşivleme:** Dosyaları sıkıştırma ve arşivleme (`**tar**`).
* **Sembolik Bağlantılar (Symlinks):** Dosya ve dizinler için kısa yollar oluşturma.

---

## 🛠️ Kullanılan Önemli Komutlar ve Kavramlar

| Alan | Komutlar / Kavramlar | Açıklama |
| :--- | :--- | :--- |
| **İzinler** | `chmod`, `rwx` | Dosya ve dizin erişim yetkilerinin yönetimi. |
| **Git** | `git commit`, `git add`, `.gitignore` | Proje değişikliklerinin takibi ve özel dosyaların göz ardı edilmesi. |
| **Fark** | `diff`, `patch` | İki metin dosyasının karşılaştırılması ve değişikliklerin uygulanması. |
| **SSH** | `ssh-keygen` | Güvenli bağlantı için anahtar çifti oluşturulması. |
| **Sistem** | `ls`, `man`, `cat`, `tar`, `find` | Dizindeki dosyaları listeleme, yardım alma ve dosya arama/arşivleme. |

---

## 📂 Proje Yapısı

Proje, 42 Okulu'nun gerektirdiği standart klasör yapısını takip etmektedir. Her alıştırma, kendi dizini içinde yer alır.
